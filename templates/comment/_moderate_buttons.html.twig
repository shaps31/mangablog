{# templates/comment/_moderate_buttons.html.twig #}
<form method="post" action="{{ path('app_comment_toggle', {id: comment.id}) }}" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('comment_toggle' ~ comment.id) }}">
    <input type="hidden" name="back" value="{{ app.request.uri }}">
    {% if comment.status == 'approved' %}
        <button class="btn btn-sm btn-outline-secondary">Annuler l’approbation</button>
    {% else %}
        <button class="btn btn-sm btn-success">Approuver</button>
    {% endif %}
</form>
